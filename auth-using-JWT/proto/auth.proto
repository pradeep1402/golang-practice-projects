syntax = "proto3";

package auth;
option go_package = "grpc-auth-jwt/gen/auth;auth";

service Auth {
  rpc Register(UserDetailRequest) returns (JwtResponse) {}
}

message UserDetailRequest{
  string email = 1;
  string password = 2;
}

message JwtResponse {
  string jwt = 1;
}